# API Docs

This section tracks developer-facing APIs and internal runtime contracts for future reference.

Most pages under this section are generated from the repository source tree by `scripts/generate_api_docs.py`.
The docs build now runs that generator automatically, and `make api-check` can be used to verify the committed API docs are current.

```{toctree}
:maxdepth: 1

generated/index
audio_tap_bus
media_runtime
```
